<% content_for :title, "Connexion Admin - Le Vélo Vert" %>

<div class="admin-login-page">
  <div class="login-container">
    <div class="login-header">
      <h1>🔐 Administration</h1>
      <p>Le Vélo Vert</p>
    </div>
    
    <%= form_with(url: admin_login_path, local: true, class: "login-form") do |f| %>
      <% if flash[:alert] %>
        <div class="form-errors">
          <%= flash[:alert] %>
        </div>
      <% end %>
      
      <div class="form-group">
        <%= label_tag :email, "Email" %>
        <%= email_field_tag :email, params[:email], required: true, placeholder: "admin@levelopert.fr", autofocus: true %>
      </div>
      
      <div class="form-group">
        <%= label_tag :password, "Mot de passe" %>
        <%= password_field_tag :password, nil, required: true, placeholder: "••••••••" %>
      </div>
      
      <div class="form-actions">
        <%= submit_tag "Se connecter", class: "btn btn-primary btn-block" %>
      </div>
    <% end %>
    
    <div class="login-footer">
      <%= link_to "← Retour au site", root_path %>
    </div>
  </div>
</div>
